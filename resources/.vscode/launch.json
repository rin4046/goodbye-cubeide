{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "STM32 Debug",
      "type": "cortex-debug",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "executable": "./Debug/${workspaceFolderBasename}.elf",
      "armToolchainPath": "${command:goodbye-cubeide.armToolchainPath}",
      "preLaunchTask": "STM32 Build",
      "runToEntryPoint": "main",
      "servertype": "stlink",
      "serverpath": "${command:goodbye-cubeide.stlinkPath}",
      "stm32cubeprogrammer": "${command:goodbye-cubeide.cubeProgrammerPath}"
    }
  ]
}
